version: "3.9"

services:

  fraud-api:

    build: .

    container_name: fraud-investigation-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./vector_db:/app/vector_db

    restart: unless-stopped
